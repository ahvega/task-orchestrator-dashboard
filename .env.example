# Dashboard container configuration

# Port exposed on the host (maps to container 8888)
DASHBOARD_PORT=8888

# Path to tasks.db inside the container (default mounted at /data)
# for local testing, create a symbolic link data pointing to the
#   actual volume path where the Docker installation of the
#   Task Orchestrator MCP Server stores its data, which is normally
#   /var/lib/docker/volumes/mcp-task-data/_data and in Windows with WSL is
#   /wsl$/c/var/lib/docker/volumes/mcp-task-data/_data
TASK_ORCHESTRATOR_DB=/data/tasks.db

# Realtime updates via WebSocket
ENABLE_WEBSOCKET=true

# Docker auto-detection is not needed inside the container (uses volume directly)
ENABLE_DOCKER_DETECTION=false

